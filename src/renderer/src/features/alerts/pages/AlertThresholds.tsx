import React from 'react'
import AlertHeader from '../Header'

const AlertThresholds = () => {
  return (
    <div className="">
      <AlertHeader title="Alert Thresholds" showItems={[]} onChange={() => { }} onSearch={() => { }} onRefetch={() => { }} onAddAlert={() => { }} />
    </div>
  )
}

export default AlertThresholds